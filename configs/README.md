# Configs

Source-controlled configuration templates for SDKWork AIoT.

| Path | Purpose |
| --- | --- |
| `configs/topology/` | Application runtime topology profile env files |

Topology profiles are referenced by `specs/topology.spec.json` and loaded by `scripts/aiot-dev.mjs`.
